import './bootstrap';
import 'alpinejs';
import { livewire_hot_reload } from 'virtual:livewire-hot-reload';
livewire_hot_reload();

// (Optional) Import Alpine if you want to manage it via Vite:
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
